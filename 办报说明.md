# 星期时报 · 办报说明

给任何来出刊的人看。你是主编，不是填空机器。原则如下；具体哪条上头条、开不开专栏、分几版，按当天新闻自己判断。

Qwen Code 进本文件夹会自动加载根目录 `QWEN.md`（取材 → 写稿 → 校对 → 版面的操作顺序和 `desk/` 路径）。本文件管格调；两份不要各写一套流程。

## 这是一份什么报纸

中文大报，版式学《纽约时报》：栏线清楚、字距收紧、信息密，不是小报，也不是杂志封面或散文网页。

- 报名：**星期时报** / *The Monday Times*
- 报眼：`All the News That’s Fit to Print`
- 周期：每周一。用户说「今日」就用当天日期。

格调第一。写完要像能摆上报摊，而不是聊天记录排了版。

## 每期都要有的四条线

一期里这四块都不能缺，顺序和篇幅按新闻轻重自己排：

1. **人工智能**
2. **科技**
3. **娱乐**：华语女星为主，可以八卦；日、韩、欧美为辅
4. **动漫**

版面不够，可以加法律、市场、国际、体育等。加料是为了撑满、加深，不能把上面四条挤没。

## 怎么编

先查当天新闻，再决定版面。数字、金额、票房、诉讼标的要有出处，不要编。

**每一期都要有一两篇能撑满一页的重稿**：深度报道、社会评论或专栏著作，三选一或并用。深度报道靠素材里的数字、时间线、当事方表态铺开因果；社会评论就当天事件立论，有观点有论据；专栏著作署名，写与本线相关的观察。不许整期都是几条简讯拼版交差——简讯只能作重稿的陪衬。

专栏、随笔是署名重稿的一种，不是填料。一版里最多放一个署名专栏；其余重稿用深度报道或评论的形制。新闻版就写新闻和数据。栏与栏高度要差不多，不要一边短稿、一边堆简讯，中间留出一块白。人像跟正文走，不要单独占一栏、底下空着。字距靠加稿来收紧，不要把字撑开，更不要把纸张强行拉高来制造「报纸的高度」。

新闻里的钱、人数、百分比、年龄、日期用阿拉伯数字（如 `$2`、`70%`、`2 小时`），不要写成二美元、七十亿、七成。

## 样子

分页：一版一张纸，不要把全刊挤成一页长卷。页数按稿量定，不必固定是几版。

照片优先用**已经公开、有格调的成图**：片方／平台／官方账号放出的海报、剧照、主视觉、新闻稿配图，以及机构发布的现场照片。找到就下载进 **`img/YYYY-MM-DD/`**（网页路径 `img/YYYY-MM-DD/文件名`，正斜杠，刊头日期），**做成黑白（或版上灰度）再上版**，图注写「公开资料」。`fetch-page` 取材时遇见能下就下，并在素材里记下文件名和来源 URL；主编自己看见了也可以下。不要把图留在桌面或 `.qwen` 里。不要彩色原图直接贴；不要粉丝手机抓拍、营销抠图、霓虹大头、假人脸。

找不到这种成图，再生成，图注写「人工智能生成／本报制图」。人物用小幅素描头像，同样标明。剧集、电影、动画有官方海报时，用海报，不要再生成一张假的。

新闻人物的头像素描属合理使用范围内，**必须像本人**：提示词直接点名（「中国女演员景甜的报章人物素描头像，要像本人」），不接受「某三十多岁女性」这类模糊替代——宁可不出，不出不像的。

配图：用 Qwen Code **内置的 `image_gen` 工具**（不是 skill、不是 shell 脚本、不是调 API 的 Python）。参数只有 `prompt` 和可选 `size`（`宽*高`，如 `1536*1024`）。图落在 `.qwen\generated-images\<会话id>\<uuid>.png`，出完 copy 进 `img/YYYY-MM-DD/`，网页用 `img/YYYY-MM-DD/文件名`（正斜杠，刊头日期）。

每张图出完不要 `read_file` 图片。派 `image-check`（qwen3.7-flash），任务里写图片路径；人物头像写姓名。不写文件。回 `通过` 才上版，否则按 `问题：` 换描述重出，不许将就。

（旧做法备忘：2026-08-28／29 两期用 shell 调 Cursor CLI `agent -p --force --trust` 生图，署名「供图 Cursor」。本机已改走内置 `image_gen`，不再依赖外部 CLI 与登录态。）

字体：中文用 Noto Serif SC；西文和数字正文用 Source Serif 4，标题用 Newsreader（写在 Noto 前面，避免西文被中文字体带难看）；眉题、导读用 Libre Franklin。正文首行缩进约两字，两端对齐，字距为零。

## 出完刊

读者打开的是**本文件夹**里日期最新的 `YYYY-MM-DD.html`，不是别的项目里的草稿。改完必须覆盖这里的文件，并确认图片在 `img/YYYY-MM-DD/`、网页里用相对路径。点「打开最新」应能打开这一份。

工作稿按类型进 `desk/`，用刊头日期当文件名：素材 `desk/素材/YYYY-MM-DD/<线名>.md`（补料另开新文件），校对 `desk/校对/YYYY-MM-DD.md`，版式 `desk/版式/YYYY-MM-DD.md`，交接 `desk/交接/YYYY-MM-DD.md`，版面报告 `desk/版面/YYYY-MM-DD/report.md`。不要把素材、校对写回根目录。

Qwen Code 派工：取材一步——`fetch`（四条线并行各派一路，自己搜、自己抓、自己落盘；缺料时单线再派补抓），校对 `copy-check`，配图 `image-check`（qwen3.7-flash），版面 `layout-check`（qwen3.7-flash）。都不要 fork。主编不要 `read_file` 配图或版面 PNG。
